# 命令行接口架构图

## 重构后的架构结构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           用户交互层                                      │
│                         (User Interface)                                 │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────┐          ┌───────────────┐         ┌────────────────────┐
│  通用代码审查  │          │  黑客松评审    │         │   未来扩展应用      │
│  (General)    │          │  (Hackathon)  │         │   (Future Apps)    │
└───────────────┘          └───────────────┘         └────────────────────┘
        │                           │                           │
        │                           │                           │
┌───────┴──────────────┐   ┌────────┴───────────────┐         │
│   Adapter Layer      │   │  Application Layer     │         │
│   (适配器层)          │   │  (应用层)               │         │
├──────────────────────┤   ├────────────────────────┤         │
│                      │   │                        │         │
│ CommandLineAdapter   │   │ HackathonCommandLine   │    ┌─────────┐
│  • main() 入口       │   │  App                   │    │ Contest │
│  • 参数解析          │   │  • main() 入口         │    │  App    │
│  • 同步/异步分析     │   │  • Git克隆             │    └─────────┘
│  • 报告生成          │   │  • 评分计算            │
│                      │   │  • 报告生成            │    ┌─────────┐
│ CommandLineInterface │   │                        │    │  Cert   │
│  • main() 入口       │   │ HackathonInteractive   │    │  App    │
│  • 交互式菜单        │   │  App                   │    └─────────┘
│  • 用户引导          │   │  • 交互式评审          │
│  • 实时反馈          │   │  • 团队管理            │
│                      │   │  • 排行榜              │
└──────────────────────┘   └────────────────────────┘
        │                           │
        │                           │
        └───────────┬───────────────┘
                    ▼
    ┌───────────────────────────────────┐
    │         Domain Layer               │
    │         (领域层)                   │
    ├───────────────────────────────────┤
    │                                   │
    │  • Project                        │
    │  • ReviewReport                   │
    │  • AnalysisTask                   │
    │  • HackathonScore                 │
    │  • Team                           │
    │  • Submission                     │
    │                                   │
    └───────────────────────────────────┘
                    │
                    ▼
    ┌───────────────────────────────────┐
    │     Infrastructure Layer          │
    │     (基础设施层)                  │
    ├───────────────────────────────────┤
    │                                   │
    │  • AI Service Adapters            │
    │  • Repository Adapters            │
    │  • File System Adapters           │
    │  • Cache Adapters                 │
    │                                   │
    └───────────────────────────────────┘
```

## 详细职责划分

### 📦 Adapter Layer (适配器层)
**位置**: `adapter.input.cli`

```
adapter/input/cli/
│
├── CommandLineAdapter.java
│   ├── 职责: 通用代码审查的命令行入口
│   ├── 特点: 参数驱动、无交互
│   └── 使用: CI/CD、脚本自动化
│
└── CommandLineInterface.java
    ├── 职责: 通用代码审查的交互式界面
    ├── 特点: 菜单驱动、用户友好
    └── 使用: 开发人员手动分析
```

**设计理念**:
- 作为**外部世界的适配器**
- 将用户输入转换为领域模型
- 不包含业务逻辑，只做适配

### 🎯 Application Layer (应用层)
**位置**: `application.hackathon.cli`

```
application/hackathon/
│
├── cli/
│   │
│   ├── HackathonCommandLineApp.java
│   │   ├── 职责: 黑客松评审的命令行入口
│   │   ├── 特点: Git集成、评分计算
│   │   └── 使用: 批量评审、自动化评分
│   │
│   └── HackathonInteractiveApp.java
│       ├── 职责: 黑客松评审的交互式应用
│       ├── 特点: 团队管理、排行榜
│       └── 使用: 评委现场评审
│
└── service/
    ├── HackathonIntegrationService.java
    ├── HackathonScoringService.java
    ├── TeamManagementService.java
    └── LeaderboardService.java
```

**设计理念**:
- 作为**特定领域的应用编排**
- 包含黑客松特定的业务逻辑
- 可以直接使用领域服务
- 独立于通用审查功能

## 数据流图

### 通用代码审查流程

```
用户输入
   │
   ├─► CommandLineAdapter (main)
   │      │
   │      ├─► 解析参数 (--project, --output)
   │      │
   │      ├─► ProjectAnalysisUseCase
   │      │      │
   │      │      └─► ProjectAnalysisService
   │      │             │
   │      │             └─► AI Service Port
   │      │
   │      └─► ReportGenerationUseCase
   │             │
   │             └─► 生成报告 (MD/HTML/JSON)
   │
   └─► CommandLineInterface (main)
          │
          ├─► 显示菜单
          ├─► 用户选择
          └─► (同上流程)
```

### 黑客松评审流程

```
用户输入
   │
   ├─► HackathonCommandLineApp (main)
   │      │
   │      ├─► 解析参数 (--github-url, --team)
   │      │
   │      ├─► RepositoryPort (克隆)
   │      │      │
   │      │      └─► GiteeAdapter / GitHubAdapter
   │      │
   │      ├─► ProjectAnalysisService (分析)
   │      │
   │      ├─► HackathonScoringService (评分)
   │      │      │
   │      │      └─► 计算多维度分数
   │      │             • 代码质量 (40%)
   │      │             • 创新性 (30%)
   │      │             • 完整性 (20%)
   │      │             • 文档 (10%)
   │      │
   │      └─► ReportGenerationService
   │             │
   │             └─► 生成评分报告
   │
   └─► HackathonInteractiveApp
          │
          ├─► 显示黑客松菜单
          ├─► 用户选择
          │      • 评审项目
          │      • 管理团队
          │      • 查看排行榜
          │
          └─► (同上流程)
```

## 对比分析

### 🔴 重构前的问题

```
CommandLineAdapter (混乱)
   │
   ├─► execute() - 通用审查
   │
   └─► executeHackathon() - 黑客松审查  ❌ 职责混乱
          │
          ├─► Git克隆逻辑  ❌ 不属于Adapter层
          ├─► 评分计算     ❌ 业务逻辑混入
          └─► 团队管理     ❌ 领域逻辑错位
```

**问题**:
1. ❌ 违反单一职责原则
2. ❌ Adapter层包含业务逻辑
3. ❌ 难以测试和维护
4. ❌ 扩展新应用困难

### 🟢 重构后的优势

```
CommandLineAdapter (清晰)
   │
   └─► execute() - 仅通用审查
          ✅ 职责单一
          ✅ 无业务逻辑
          ✅ 易于测试

HackathonCommandLineApp (独立)
   │
   └─► execute() - 黑客松评审
          ✅ 完整的业务流程
          ✅ 位于Application层
          ✅ 可独立扩展
```

**优势**:
1. ✅ 符合六边形架构
2. ✅ 职责清晰分离
3. ✅ 易于测试和维护
4. ✅ 扩展新应用简单

## 扩展性示例

### 添加新应用：编程竞赛

```
application/contest/
│
├── cli/
│   ├── ContestCommandLineApp.java      # 🆕 新应用入口
│   │      │
│   │      ├─► 解析参数 (--contest-id, --participant)
│   │      ├─► 提交验证
│   │      ├─► 自动评分
│   │      └─► 实时排名
│   │
│   └── ContestInteractiveApp.java
│          │
│          └─► 交互式竞赛管理
│
└── service/
    ├── ContestManagementService.java
    ├── SubmissionValidationService.java
    └── LiveRankingService.java
```

**不影响现有功能**:
- ✅ 通用代码审查继续工作
- ✅ 黑客松评审继续工作
- ✅ 三个应用完全独立

---

**架构原则**: 低耦合、高内聚、易扩展  
**设计模式**: 六边形架构、依赖注入、端口适配器  
**版本**: 2.0

