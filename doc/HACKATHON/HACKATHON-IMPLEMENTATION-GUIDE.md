# Hackathon AI 工具实施指南

## 📋 概述

Hackathon-AI 是一个企业级 AI 驱动的智能评审引擎，专为黑客松项目评审设计。支持多模态项目分析，包括 Git 仓库和本地目录两种输入方式。

## 🎯 核心功能

### 1. 多源输入支持
- **GitHub 仓库**: 支持直接从 GitHub URL 克隆并分析
- **Gitee 仓库**: 支持直接从 Gitee URL 克隆并分析
- **本地目录**: 支持分析本地项目目录

### 2. 评分维度
黑客松评分包含以下维度：
- **代码质量** (40%权重): 代码规范、可维护性、设计模式
- **创新性** (30%权重): 技术创新、解决方案独特性
- **完整性** (20%权重): 功能完整度、项目成熟度
- **文档质量** (10%权重): README、注释、文档完善度

总分计算公式：
```
总分 = 代码质量 × 0.4 + 创新性 × 0.3 + 完整性 × 0.2 + 文档质量 × 0.1
```

等级评定：
- S级: 90-100分 (优秀)
- A级: 80-89分 (良好)
- B级: 70-79分 (中等)
- C级: 60-69分 (及格)
- D级: 50-59分 (较差)
- F级: 0-49分 (不及格)

## 🏗️ 构建与部署

### 1. 构建 JAR 文件

#### 方式一：使用批处理脚本（推荐）
```bash
hackathon-ai_buildStart.bat
```

#### 方式二：使用 Maven 命令
```bash
mvn clean package -DskipTests -f hackathon-ai.xml
```

构建完成后，JAR 文件位置：`target/hackathon-ai.jar`

### 2. 依赖要求
- **Java**: JDK 17 或更高版本
- **Maven**: 3.6+ (仅构建时需要)
- **内存**: 建议 2GB+ 可用内存

## 🚀 使用指南

### 命令行接口

#### 1. 查看帮助信息
```bash
# 查看主帮助
java -jar target/hackathon-ai.jar --help

# 查看黑客松模式帮助
java -jar target/hackathon-ai.jar hackathon --help
```

#### 2. GitHub 仓库模式
```bash
java -jar target/hackathon-ai.jar hackathon \
  --github-url https://github.com/user/project \
  --team "Team Awesome" \
  --branch main \
  --output score.json \
  --report report.md
```

#### 3. Gitee 仓库模式
```bash
java -jar target/hackathon-ai.jar hackathon \
  --gitee-url https://gitee.com/user/project \
  --team "Team Awesome" \
  --branch main \
  --output score.json \
  --report report.md
```

#### 4. 本地目录模式
```bash
java -jar target/hackathon-ai.jar hackathon \
  --directory /path/to/project \
  --team "Team Awesome" \
  --output score.json \
  --report report.md
```

### 交互式评审（使用批处理脚本）

#### Windows 用户
```bash
hackathon-ai-score.bat
```

脚本会引导您完成以下步骤：
1. 选择评审模式（Git URL 或本地目录）
2. 如果选择 Git URL，选择仓库类型（GitHub 或 Gitee）
3. 输入相关信息（URL/路径、团队名称、分支等）
4. 自动执行评审并生成报告

## 📊 输出结果

### 1. 评分结果 (score.json)
JSON 格式的评分数据，包含：
- 各维度详细评分
- 总分和等级
- 项目基本信息
- 评审时间戳

示例：
```json
{
  "team": "Team Awesome",
  "totalScore": 85,
  "grade": "A",
  "dimensions": {
    "codeQuality": 88,
    "innovation": 82,
    "completeness": 85,
    "documentation": 80
  },
  "timestamp": "2025-11-12T13:30:00"
}
```

### 2. 详细报告 (report.md)
Markdown 格式的详细分析报告，包含：
- 项目概览
- 各维度详细分析
- 优势与改进建议
- 代码质量指标
- 项目结构分析

## 🔧 配置说明

### 1. AI 服务配置
配置文件位置：`src/main/resources/config.yaml`

支持的 AI 服务：
- DeepSeek
- OpenAI
- Claude (Anthropic)
- Gemini (Google)
- AWS Bedrock
- 通义千问 (Alibaba)
- 文心一言 (Baidu)

配置示例：
```yaml
ai:
  provider: deepseek
  apiKey: ${DEEPSEEK_API_KEY}
  model: deepseek-chat
  temperature: 0.3
  maxTokens: 4000
```

### 2. 文件系统配置
可配置扫描规则：
- **包含模式**: `*.java`, `*.py`, `*.js`, `*.ts`, `*.xml`, `*.yaml`, `*.md`
- **排除模式**: `*test*`, `*.class`, `*.jar`, `node_modules`, `target`, `build`
- **最大文件大小**: 1024 KB
- **扫描深度**: 4 级目录

## 🏗️ 架构设计

### 六边形架构 (Hexagonal Architecture)
```
┌─────────────────────────────────────────┐
│         外部适配器 (Adapters)            │
├─────────────────────────────────────────┤
│ • CLI 适配器 (CommandLineAdapter)        │
│ • GitHub 适配器 (GitHubAdapter)          │
│ • Gitee 适配器 (GiteeAdapter)            │
│ • 文件系统适配器 (FileSystemAdapter)     │
│ • AI 服务适配器 (AIAdapter)              │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│         应用层 (Application)             │
├─────────────────────────────────────────┤
│ • 黑客松集成服务                         │
│ • 项目分析服务                           │
│ • 评分计算服务                           │
│ • 报告生成服务                           │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│         领域层 (Domain)                  │
├─────────────────────────────────────────┤
│ • HackathonScore (评分模型)              │
│ • HackathonProject (项目模型)            │
│ • ReviewReport (评审报告)                │
│ • Team (团队模型)                        │
└─────────────────────────────────────────┘
```

### 关键组件

#### 1. CommandLineAdapter
- **职责**: CLI 入口，参数解析，命令分发
- **支持命令**: 
  - `hackathon`: 黑客松评审模式
  - (默认): 标准项目分析模式

#### 2. GitHubAdapter / GiteeAdapter
- **职责**: Git 仓库操作（克隆、元数据获取）
- **技术**: JGit
- **功能**: 浅克隆、分支选择、提交历史分析

#### 3. HackathonScoringService
- **职责**: 计算黑客松评分
- **输入**: ReviewReport, Project
- **输出**: HackathonScore
- **算法**: 加权评分模型

## 📝 开发说明

### 项目结构
```
src/
├── main/
│   ├── java/top/yumbo/ai/reviewer/
│   │   ├── adapter/
│   │   │   ├── input/
│   │   │   │   ├── cli/           # CLI 适配器
│   │   │   │   └── hackathon/     # 黑客松适配器
│   │   │   └── output/            # 输出适配器
│   │   ├── application/           # 应用服务
│   │   │   └── hackathon/         # 黑客松服务
│   │   └── domain/                # 领域模型
│   │       └── hackathon/         # 黑客松模型
│   └── resources/
│       ├── config.yaml            # 配置文件
│       └── prompts/               # AI 提示词
└── test/                          # 测试代码
```

### 扩展指南

#### 添加新的 Git 平台支持
1. 实现 `GitHubPort` 接口
2. 在 `CommandLineAdapter.detectGitRepositoryAdapter()` 添加检测逻辑
3. 在 `hackathon-ai-score.bat` 添加新选项

#### 添加新的评分维度
1. 更新 `HackathonScore` 模型
2. 修改 `calculateHackathonScore()` 方法
3. 更新权重常量

#### 集成新的 AI 服务
1. 实现 `AIServicePort` 接口
2. 添加配置支持
3. 在 `CommandLineAdapter.createAIAdapter()` 添加初始化逻辑

## 🐛 故障排查

### 常见问题

#### 1. JAR 文件不存在
**错误**: `Error: Unable to access jarfile target\hackathon-ai.jar`

**解决方案**:
```bash
# 重新构建
hackathon-ai_buildStart.bat
```

#### 2. 内存不足
**错误**: `OutOfMemoryError`

**解决方案**:
```bash
# 增加 JVM 内存
java -Xmx4g -jar target/hackathon-ai.jar hackathon ...
```

#### 3. Git 克隆失败
**错误**: `Failed to clone repository`

**可能原因**:
- 网络连接问题
- 仓库不存在或无访问权限
- Git URL 格式错误

**解决方案**:
- 检查网络连接
- 确认仓库 URL 正确
- 尝试使用本地目录模式

#### 4. AI 服务调用失败
**错误**: `AI service unavailable`

**解决方案**:
- 检查 `config.yaml` 配置
- 确认 API Key 有效
- 检查网络代理设置

## 📈 性能优化

### 1. Git 克隆优化
- 使用浅克隆（depth=1）减少下载时间
- 仅克隆指定分支
- 克隆后自动清理临时文件

### 2. 文件扫描优化
- 排除不必要的文件类型
- 限制扫描深度
- 并行处理多个文件

### 3. AI 调用优化
- 结果缓存机制
- 批量分析减少 API 调用
- 异步处理提高并发

## 🔐 安全说明

### 1. API 密钥管理
- 使用环境变量存储敏感信息
- 不要将密钥提交到版本控制
- 定期轮换 API 密钥

### 2. 代码安全
- 临时克隆的代码会自动清理
- 不会上传或泄露项目代码
- 本地处理，保护隐私

## 📞 支持与反馈

### 问题报告
如遇到问题，请提供：
- 完整的错误消息
- 使用的命令
- Java 版本信息
- 操作系统信息

### 功能建议
欢迎提出新功能建议或改进意见。

## 📄 许可证

本项目采用开源许可证，详见 LICENSE.txt 文件。

---

**版本**: 2.0  
**更新日期**: 2025-11-12  
**作者**: AI-Reviewer Team

